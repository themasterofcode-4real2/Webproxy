{
  "name": "fastify-proxy",
  "version": "1.0.0",
  "description": "A simple web proxy using Fastify",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "fastify": "^3.0.0",
    "fastify-http-proxy": "^6.2.1",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=16"
  }
}
